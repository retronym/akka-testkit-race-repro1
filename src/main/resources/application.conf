# Whether Bootstrap.onStartup() drives the entity burst, and how many commands it sends. A test
# overrides this per arm through the TestKit's additional config.
repro.onstartup-burst {
  enabled = true
  size = 8
}
