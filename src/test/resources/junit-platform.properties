# A cycle that blocks should fail with its own name attached rather than run until the job is
# cancelled. Fifteen cycles of start and stop take a few minutes when healthy.
junit.jupiter.execution.timeout.testable.method.default = 20 m
junit.jupiter.execution.timeout.mode = disabled_on_debug
